// 弹幕相关定义

package Common;

enum DmMessageType
{
    SCROLL_DM   = 0; // 滚动弹幕
    BOTTOM_DM   = 1; // 底部弹幕
    TOP_DM      = 2; // 顶部弹幕
}

// 前端自己填吧
enum DmMessageColour
{
    color0   = 0;
    color1   = 1;
	color2   = 2;
	color3   = 3;
	color4   = 4;
	color5   = 5;
	color6   = 6;
}

message DmUsedInfo
{
    optional uint32 scene_id         = 1;// 场景id
    optional sint32 general_dm_used  = 2;// 普通已用次数
    optional sint32 vip_dm_used      = 3;// vip弹幕已用次数
}
